[user]
        email = neal.anglo@gmail.com
        name = ackneal
[core]
        editor = vim
        excludesfile = ~/.gitignore
[alias]
        ci = commit
        st = status
        co = checkout
        br = branch
        lg = log --all --graph --abbrev-commit --date=iso --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ad)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)'
